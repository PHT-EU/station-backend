http:
  routers:
    traefik:
      rule: "Host(`traefik.station.localhost`)"
      service: "api@internal"
      tls:
        domains:
          - main: "station.localhost"
            sans:
              - "*.station.localhost"


tls:
  certificates:
    
    - certFile: "/etc/certs/C:\Users\Michael Graf\pht\station\certs\cert.pem"
      keyFile: "/etc/certs/C:\Users\Michael Graf\pht\station\certs\key.pem"
    
